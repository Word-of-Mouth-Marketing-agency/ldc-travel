DATABASE_URL=
PAYLOAD_SECRET=
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_LAUNCH_MARKET_CODE=

# Next reads the HTTP port before application code starts; provide it from the process manager/shell.
# PORT=

# Optional persistent directory for Payload uploads. Keep it outside release directories in production.
# PAYLOAD_MEDIA_DIR=

# Set true only for a temporary database-free Vercel UI preview. Never enable on the real production VPS site.
UI_PREVIEW_MODE=
